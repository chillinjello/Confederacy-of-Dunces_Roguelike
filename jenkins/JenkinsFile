pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo '---- Begin Checkout ----'
            }
            checkout smc
            sh 'ls -a'
        }
    }
}