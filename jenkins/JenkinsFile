pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo '---- Begin Checkout ----'
            }
            sh 'ls -a'
            checkout smc
            sh 'ls -a'
        }
    }
}